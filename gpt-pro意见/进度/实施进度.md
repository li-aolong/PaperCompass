# PaperCompass 按 GPT-Pro 评审改进进度

> 权威评审文件：`../gpt_pro_raw_review.md`
> 本目录是本轮改进的进度目录；后续每个阶段性改动、验证和对比测试都同步记录在这里。

状态图标：⬜ 未开始 · 🟦 进行中/部分完成 · ✅ 完成并已验证 · ⏸ 暂缓

## 总览

| 阶段 | 覆盖评审点 | 状态 | 说明 |
| --- | --- | --- | --- |
| S0 | 元数据融合、召回噪声、预算门控 | ✅ | 当前分支已完成上一批修复：`publication_status`、in-scope 方向拆解、通用召回词 denylist、weak batch 自动提批等。旧记录原位于 `方案与进度/`，本轮迁移到本目录作为权威进度。 |
| S1 | Local-First 写入安全、默认凭证解析、Gemini 年份 bug、单主 Agent 文案 | ✅ | 已完成并通过全量测试：`258 passed, 3 deselected`。 |
| S2 | GPT-Pro P0 其余项：代码级 Confirmation Gate、catalog 事务 swap、测试失败收敛 | ✅ | catalog 事务 swap、QA coverage 只读模式、代码级 Confirmation Gate 均已完成并通过全量测试。 |
| S3 | 结构性演进：source plugin、确定性 prefilter、单 Agent review schema、增量 update | ✅ | 已完成低风险切口、统一写锁/原子写、两阶段 confirmation、prefilter/review cache v2、OpenAI-compatible Brain、source registry/preflight、arXiv/OpenAlex 插件级 plan/fetch/normalize、checkpointed update、doctor/monitor/metrics 与源码包扫描；第四轮 P0/P1 与收口项已全量验证。 |
| S4 | 两个既有 workspace 对比测试 | ✅ | 已对 ICL 2025+ 与 multi-agent systems 2025+ 重跑 build/catalog/QA，并刷新 final summary。 |
| S5 | 第五轮 GPT-Pro 收尾：archive strict doctor、全局锁 deadline、prefilter 分层审计、boundary partial v2、update rollback、arXiv 结构化 runner、doctor/metrics 增强 | ✅ | Round 5 全部建议已落地并验证：当前工作树 `315 passed, 3 deselected`；源码包 strict doctor passed；解压源码包 `311 passed, 4 skipped, 3 deselected`。 |
| S6 | 第六轮 GPT-Pro hardening：doctor fix 加锁/年龄阈值、staged update、metrics 贯通、低层写锁、OpenAlex/Semantic Scholar structured runner、archive safe extraction/path traversal、release manifest、monitor trends/retention | ✅ | Round 6 全部建议已落地并验证：当前工作树 `322 passed, 3 deselected`；源码包 normal/strict doctor passed；解压源码包 `318 passed, 4 skipped, 3 deselected`。 |
| S7 | 第七轮 GPT-Pro release hardening：checkpoint failure rollback、源码包 symlink 防护、doctor trash orphan 排除、nested Zip Slip 回归 | ✅ | Round 7 建议已落地并验证：当前工作树 `326 passed, 3 deselected`；源码包 normal/strict doctor passed；解压源码包 `322 passed, 4 skipped, 3 deselected`。 |

## 阶段记录

### 2026-06-19 S0：元数据 / 召回 / 预算门控修复（已在当前分支）

- ✅ `normalize.py`：按源可信度收尾选择 venue，并派生 `publication_status`，降低 paperlists/OpenReview 拒稿记录覆盖权威发表记录的风险。
- ✅ `scope.py`：新增 `acceptance` 策略门，默认 `accepted_or_preprint`，拒稿/撤稿不会冒充正式收录。
- ✅ `auto/plan.py`：axis 检测只看 in-scope 文本；新增通用召回词 denylist，避免 `scaling laws`、`bayesian inference` 这类宽泛词单独成为召回锚点。
- ✅ `auto/quality_gate.py` / `auto/orchestrator.py`：把大而干净的 weak queue 归为预算问题，并在安全上限内自动提高 `weak_max_batches`。
- ✅ `auto/stages.py`：修正 batch 进度显示为真实 `i/N`。
- ⏸ 暂缓：联网召回词体检、预算 resume、完整实体融合层。原因是测试稳定性和改动面较大，先由已完成的局部修复覆盖观测到的主要问题。

### 2026-06-19 S1：Local-First 与 source 可靠性修复（完成）

- ✅ `text.py`：`write_json()` / `write_jsonl()` 改为同目录临时文件写入、`fsync` 后 `os.replace()` 原子替换；新增 `append_jsonl_locked()`，用旁路 `.lock` 文件保护 JSONL 追加。
- ✅ `discovery.py`：`append_jsonl()` 改走 `append_jsonl_locked()`；新增 `resolve_secret()`，默认读取 `OPENALEX_API_KEY`、`OPENALEX_EMAIL`、`NCBI_API_KEY`、`SEMANTIC_SCHOLAR_API_KEY`，修正文档与代码不一致。
- ✅ `candidate_review.py`：`applied_decisions.jsonl` 改用锁定追加，降低并发 apply 损坏审计日志的风险。
- ✅ `discovery.py`：`run_discovery()` 调用 `sync_gemini_search()` 时传 `(min_year, max_year)` 二元年份窗口，修复真实调用链把年份列表传给 Gemini Search 的 bug。
- ✅ `cli.py` / `auto/orchestrator.py`：把 `--second-brain` 文案改成高级可选，不再把 cross-model 作为默认推荐心智。
- ✅ `tests/test_discovery.py`：新增默认 env secret 和 Gemini 年份窗口回归测试。
- ✅ 验证：相关单测 `6 passed`；加入 S2 catalog / QA 测试后，全量 `uv run --no-sync pytest -q` 结果为 `258 passed, 3 deselected`。

### 2026-06-19 S2a：catalog 事务式切换（完成）

- ✅ `catalog.py`：`catalog build` 不再先删除旧 `catalog/`。现在先生成唯一 `.catalog.tmp.<id>/`，成功写完后把旧 catalog 改名为 `.catalog.prev.<id>/`，再把新目录切到 `catalog/`。
- ✅ 故障恢复：如果新目录切换失败，会把 `.catalog.prev.<id>/` 恢复回 `catalog/`，避免崩溃后 workspace 没有可用 catalog。
- ✅ `tests/test_portable_paths.py`：新增 `test_catalog_build_restores_previous_catalog_when_swap_fails`，模拟切换失败并验证旧 manifest 被恢复。
- ✅ 验证：相关测试 `2 passed`；全量 `257 passed, 3 deselected`。
- ✅ 真实 workspace 复测：对 ICL 2025+ 与 multi-agent systems 2025+ 重新运行新 `catalog build` + `qa workspace --refresh-summary`，计数保持 20 / 193，QA 仍为 warning 且无 critical。

### 2026-06-19 S2b：QA coverage 刷新改为显式（完成）

- ✅ `qa.py`：`build_quality_report(..., refresh_coverage=False)` 默认只读 `.papercompass/manifests/coverage_report.json`；只有传 `refresh_coverage=True` 时才调用 `make_coverage_report()` 刷新 coverage manifest。
- ✅ `cli.py`：`papercompass qa workspace` 新增 `--refresh-coverage`，默认 QA 不再重写 coverage manifest；`--refresh-summary` 保持只刷新 final summary。
- ✅ `docs/commands.md`：补充 QA 默认只读 coverage，以及需要同步刷新时使用 `--refresh-coverage`。
- ✅ `tests/test_qa.py`：新增默认不刷新 coverage 的回归测试，并把 stale coverage 刷新测试改为显式 `refresh_coverage=True`。
- ✅ 验证：QA 相关测试 `3 passed`；全量 `258 passed, 3 deselected`。
- ✅ 真实 workspace 复测：默认 `qa workspace` 下，ICL 与 multi-agent 的 `coverage_manifest.refresh_status` 均为 `not_refreshed`、`count_matches=true`，QA 仍无 critical。

### 2026-06-19 S2c：代码级 Confirmation Gate（完成）

- ✅ `auto/orchestrator.py`：新增 `ConfirmationRequired`，正式 `run_auto_build(..., plan_only=False)` 默认要求 `user_confirmed=True`；未确认时在选择 brain 和创建 workspace 前直接失败。
- ✅ `cli.py`：`papercompass auto-build` 新增 `--user-confirmed`；正式运行必须由调用方显式声明已完成 AGENT_ENTRY.md 用户确认门，`--plan-only` 预览可不传。
- ✅ `README.md` / `AGENT_ENTRY.md` / `docs/auto-build.md` / `docs/commands.md` / `docs/core-pipeline.md`：正式构建示例统一补 `--user-confirmed`。
- ✅ OpenAlex 文案收敛：移除未在本轮核验的具体日期、免费额度和绝对政策断言，改成“匿名或未认证访问更易遇到 403/429、限速或配额问题”的风险提示。
- ✅ `tests/test_auto_orchestrator.py`：新增未确认正式构建阻断、`plan-only` 不要求确认的回归测试。
- ✅ 验证：`uv run --no-sync pytest -q tests/test_auto_orchestrator.py` 为 `26 passed`；`uv run --no-sync pytest -q` 为 `260 passed, 3 deselected`；`papercompass auto-build --help` 已显示 `--user-confirmed`。

### 2026-06-19 S3a：溯源、Web 分级显示与中间 catalog 收敛（完成）

- ✅ `auto/plan.py` / `auto/stages.py` / `auto/orchestrator.py` / `cli.py`：新增 `--original-query`，将用户原始请求原话原样写入 `topic.yaml.original_query`，同时记录到 auto state；`direction_raw` 继续保存 Agent 提炼后的正式方向。
- ✅ `auto/stages.py`：`stage_discover()` 在 auto 流程里保留 discovery 后 build（供 seed_check/QA 使用），但关闭中间 `catalog` 生成，避免正式流程完成前出现中间态 catalog。
- ✅ `web.py` / `static/app.js` / `static/app.css` / `static/index.html`：Web API 暴露 `paper_role`、`role_label`、`decision_label`；列表与详情页显示 Core / Eval / Anchor / Negative 等角色 badge；筛选栏新增 Role 过滤。
- ✅ `docs/*` / `README.md` / `AGENT_ENTRY.md`：补充 `--original-query` 和 `topic.yaml.original_query` 的使用说明。
- ✅ `tests/test_auto_plan.py`：新增 `original_query` 原样持久化测试。
- ✅ `tests/test_auto_orchestrator.py`：新增 auto discovery 保留 build、跳过中间 catalog 的回归测试。
- ✅ `tests/test_web.py`：新增 Web role badge 字段、role filter/search 回归测试。
- ✅ 验证：目标测试 `uv run --no-sync pytest -q tests/test_auto_plan.py tests/test_auto_orchestrator.py tests/test_web.py` 为 `56 passed`；`node --check src/papercompass/static/app.js` 通过；`uv run --no-sync pytest -q` 为 `264 passed, 3 deselected`。

### 2026-06-19 S3b：Normalization 身份键与 decision merge 收敛（完成）

- ✅ `normalize.py`：`identity_keys()` 不再输出裸 `title:<normalized>` 身份键，只保留强 ID 与 `title:<normalized>:<year>`，降低同名/泛名论文跨年份误合并风险；review 匹配仍由 `candidate_match_keys()` 保留裸 title 兼容。
- ✅ `normalize.py`：新增 decision priority merge。`required_seed`、`trusted_import`、hard reject 等高优先级 decision 不会被 `pending_fusion_score` / weak decision 覆盖；included decision 会清理低优先级 pending 残留的 `needs_review`。
- ✅ `tests/test_normalize.py`：新增裸 title key 移除、同题不同年份不误合并、trusted decision 不被 pending 覆盖的回归测试。
- ✅ 验证：normalize/build/review/QA 相关目标测试 `55 passed`；`uv run --no-sync pytest -q` 为 `267 passed, 3 deselected`。

### 2026-06-19 S3c：显式年份门（完成）

- ✅ `discovery.py`：`year_range()` 不再在 `min_year` 与 `topic.yaml.min_year` 均缺失时静默回退到 2022，而是直接报错，避免违背 SOP 的“不要编造年份”。
- ✅ `tests/test_discovery.py`：新增缺失 min_year 报错测试。
- ✅ 验证：discovery/CLI/auto 相关目标测试 `75 passed`；`uv run --no-sync pytest -q` 为 `268 passed, 3 deselected`。

### 2026-06-19 S3d：单 Agent review schema 增强（完成）

- ✅ `auto/prompts.py`：brain score schema 增加 `confidence`、`inclusion_evidence`、`exclusion_evidence`、`missing_information`，要求单主 brain 给出结构化证据和缺失信息；数组可为空，适配 strict schema。
- ✅ `auto/brain_score.py`：解析并清洗新增字段，旧格式缺字段时仍兼容。
- ✅ `auto/stages.py`：score partial cache、`score_decisions_*.jsonl`、`review_decisions_*.jsonl` 和 boundary resolved decisions 透传新增 review 字段。
- ✅ `candidate_review.py` / `build.py`：手工或自动 review decision 的新增字段会进入 `applied_decisions.jsonl`，并保留到最终 paper 的 `review_decision`。
- ✅ `tests/test_score_pipeline.py` / `tests/test_candidate_review.py`：覆盖 score/review/applied/final paper 的字段保留。
- ✅ 验证：score/review 相关目标测试 `38 passed`；`uv run --no-sync pytest -q` 为 `268 passed, 3 deselected`。

### 2026-06-19 S3e：保守增量 update 入口（完成）

- ✅ `update.py`：新增 `run_workspace_update()`，在 workspace 级 `update.lock` 内串联 `discover(build=False,catalog=False)`、`build_workspace()`、`build_catalog()` 和 `build_quality_report()`，并写入 `.papercompass/updates/update_<ts>.json` 与 `.papercompass/updates/latest.json`。
- ✅ `update.py` / `cli.py`：正式 update 新增代码级确认门，缺少 `--user-confirmed` 时在修改 workspace 前失败。
- ✅ `cli.py`：新增 `papercompass update` 子命令，复用 discover source choices，支持 `--sources`、年份窗口、`--refresh`、`--max-remote-calls`、`--refresh-coverage`。
- ✅ `README.md` / `AGENT_ENTRY.md` / `docs/commands.md` / `docs/core-pipeline.md`：补充已有 workspace 刷新的推荐入口，明确当前是保守全刷新，不承诺未经验证的 delta-only 合并。
- ✅ `tests/test_update.py`：覆盖确认门零副作用、full-refresh 调用参数、summary 写入和 CLI 未确认报错。
- ✅ 验证：`uv run --no-sync pytest -q tests/test_update.py` 为 `3 passed`；`uv run --no-sync papercompass update --help` 正常；`uv run --no-sync pytest -q` 为 `271 passed, 3 deselected`；`git diff --check` 无输出。

### 2026-06-19 S3f：确定性 prefilter 排序与诊断层（完成）

- ✅ `auto/prefilter.py`：新增 `PrefilterPipeline`，从 `source_filter_terms`、`discriminator_terms`、`search_hints`、`search_keyword_text` 与 out-of-scope examples 中提取正/负信号，计算确定性 `prefilter_score`、topic hits、negative hits 与 reasons。
- ✅ `auto/stages.py`：`score_papers` 在进入 brain 前计算 prefilter summary；当 weak review 预算有限时，用 embedding 分数 + prefilter 分数排序候选，required seed 仍优先保护。
- ✅ `auto/stages.py`：`score_decisions_*.jsonl` 新增 `prefilter_action`、`prefilter_score`、`prefilter_reasons`、`prefilter_topic_hits`、`prefilter_negative_hits`，便于后续 QA/cost 分析。
- ✅ 行为边界：本阶段不直接自动 accept/reject，不改变 full-coverage 情况下的候选覆盖；只做排序和可解释诊断。
- ✅ `docs/core-pipeline.md` / `docs/auto-build.md`：补充 prefilter 位置和行为边界。
- ✅ `tests/test_prefilter.py` / `tests/test_score_pipeline.py`：覆盖 prefilter 自身和 score decision 字段保留。
- ✅ 验证：`uv run --no-sync pytest -q tests/test_prefilter.py` 为 `2 passed`；`uv run --no-sync pytest -q tests/test_score_pipeline.py` 为 `8 passed`；`uv run --no-sync pytest -q` 为 `273 passed, 3 deselected`；`git diff --check` 无输出。

### 2026-06-19 S3g：Source plugin registry / protocol 骨架（部分完成）

- ✅ `sources/registry.py`：新增 `SourceDefinition`、`SourcePlugin` protocol、`SourceRegistry` 与内置 source registry，作为后续拆分 discovery 单体调度器的稳定入口。
- ✅ `cli.py`：`discover`、`update`、`auto-build --sources` 的 choices 改从 `default_source_registry().names()` 获取，避免 CLI source 名单和 discovery 漂移。
- ✅ `discovery.py`：在联网和写 raw 之前校验未知 source，避免拼错 source 名称时静默跳过。
- ✅ `docs/core-pipeline.md`：补充 source registry 现状和后续迁移边界。
- ✅ `tests/test_source_registry.py`：覆盖内置 source 列表、重复注册和 discovery 未知 source 报错。
- ✅ 验证：`uv run --no-sync pytest -q tests/test_source_registry.py` 为 `3 passed`；`papercompass discover --help` 与 `papercompass update --help` source choices 正常；`uv run --no-sync pytest -q` 为 `276 passed, 3 deselected`；`git diff --check` 无输出。
- ⏸ 后续：arXiv / OpenAlex 的插件级 query planning、fetch、normalize 已在 S3i 补齐；其他内置 source 的 fetch/cache/raw/coverage 逻辑仍主要在 `discovery.py`，需要按 Semantic Scholar -> Crossref/DBLP/Paperlists -> 领域源逐步迁移。

### 2026-06-20 S3h：第二轮 P0 安全层收尾（完成）

- ✅ `text.py`：`atomic_write_text()` / `write_jsonl()` 在 `os.replace()` 或写入异常时清理同目录 `.tmp` 文件；文件锁新增 Windows `msvcrt` fallback，并在缺锁平台显式失败，不再静默无锁。
- ✅ `text.py`：新增进程内可重入的 workspace 级互斥锁，锁文件放在系统临时目录，不污染 workspace，也不影响 auto-build 的“未确认零副作用”契约。
- ✅ `config.py` / `auto/state.py`：`write_yaml()`、auto `state.json` 改为原子写；auto iteration log 改为锁定追加。
- ✅ `build.py` / `discovery.py` / `catalog.py` / `update.py` / `qa.py` / `candidate_review.py` / `auto/orchestrator.py`：主要写 workspace 入口接入统一 workspace 锁。`catalog build` 成功事务切换后清理旧 catalog 备份。
- ✅ `auto/orchestrator.py`：正式 auto-build 的 confirmation gate 提前到获取锁之前，保持未确认时不创建 workspace。
- ✅ `docs/core-pipeline.md`：补充 workspace 锁与关键产物原子写约定。
- ✅ `tests/test_text_storage.py`：新增临时文件异常清理、workspace lock 可重入且不污染 workspace 的回归测试。
- ✅ 验证：相关测试 `59 passed`；全量 `uv run --no-sync pytest -q` 为 `279 passed, 3 deselected`；`git diff --check` 无输出。

## 待验证

- [x] 运行与本阶段相关的单元测试。
- [x] 运行全量 `pytest -q`，记录通过/失败数量。
- [x] 对 ICL 2025+ workspace 重跑 build/catalog/QA 并记录前后差异。
- [x] 对 multi-agent systems 2025+ workspace 重跑 build/catalog/QA 并记录前后差异。

### 2026-06-19 S4：两个既有 workspace 对比测试（完成）

重跑命令：

```bash
uv run --no-sync papercompass build --workspace <workspace>
uv run --no-sync papercompass catalog build --workspace <workspace>
uv run --no-sync papercompass qa workspace --workspace <workspace> --refresh-summary
```

| Workspace | 重跑前主库 / pending / rejected | 重跑后主库 / anchor / pending / rejected | Catalog | QA | 备注 |
| --- | --- | --- | --- | --- | --- |
| `in-context-learning-icl-in-large-language-models-how-llms-learn-from-dem--2025plus` | 73 / 0 / 1390 | 20 / 16 / 0 / 1443 | 20 | `warning`，无 critical；warning=`source_coverage_has_risk` | S0 的 `publication_scope_gate` 生效，剔除 53 条未确认接收/拒稿记录；`summary_refresh` 已把 final summary 从 73 篇刷新到 20 篇。 |
| `multi-agent-systems--2025plus` | 193 / 0 / 150 | 193 / 55 / 0 / 150 | 193 | `warning`，无 critical；warnings=`source_coverage_has_risk`, `source_auth_failed_optional` | 主库数量稳定；warning 主要来自 Semantic Scholar API key 被官方 403 拒绝，非本轮代码回归。 |

结论：本轮代码改动没有破坏离线 build/catalog/QA；ICL 库的变化来自已实现的接收状态/发表范围门禁，是预期的质量收敛。

### 2026-06-20 S4b：安全层收尾后的 workspace 复测（完成）

重跑命令同 S4：

```bash
uv run --no-sync papercompass build --workspace <workspace>
uv run --no-sync papercompass catalog build --workspace <workspace>
uv run --no-sync papercompass qa workspace --workspace <workspace> --refresh-summary
```

| Workspace | 重跑前 final_summary | 重跑后 final_summary / catalog | QA | 备注 |
| --- | --- | --- | --- | --- |
| `in-context-learning-icl-in-large-language-models-how-llms-learn-from-dem--2025plus` | 20 / 16 / 0 / 1443 | 20 / 16 / 0 / 1468；catalog=20 | `warning`，无 critical；warning=`source_coverage_has_risk` | 主库与 anchor 不变；`--refresh-summary` 把 stale rejected 计数校准为当前 build 的 1468。 |
| `multi-agent-systems--2025plus` | 193 / 55 / 0 / 150 | 193 / 55 / 0 / 150；catalog=193 | `warning`，无 critical；warnings=`source_coverage_has_risk`, `source_auth_failed_optional` | 计数稳定；Semantic Scholar key 仍被官方 403 拒绝，属于已有外部 source 风险。 |

结论：统一 workspace 锁、原子写和 catalog 备份清理没有破坏两个既有库的离线重建与 QA。

### 2026-06-21 S3i：第四轮 P0/P1 安全与运维收口（完成）

- ✅ `confirmation.py` / `auto/orchestrator.py` / `cli.py`：`auto-build` confirmation inputs 纳入 `fresh`，并新增 `workspace_context_hash(topic.yaml + sources.yaml)`；`update` 与低层 mutating command 同样绑定 workspace 配置上下文，避免确认后参数或配置漂移。
- ✅ `text.py` / `cli.py`：workspace/file lock 增加默认 timeout（`PAPERCOMPASS_LOCK_TIMEOUT_SECONDS`），CLI 结构化输出 `workspace_lock_timeout`；`write_json()` 末尾补换行。
- ✅ `plugins/brain.py` / `auto/review_cache.py`：`OpenAICompatibleBrain` 支持 `PAPERCOMPASS_BRAIN_RESPONSE_FORMAT=json_object|json_schema|none`、`PAPERCOMPASS_BRAIN_MAX_TOKENS`、429/5xx transient backoff；review cache key 纳入 model revision，cache 损坏行默认 quarantine，不阻断 build。
- ✅ `auto/stages.py` / `tests/test_score_pipeline.py`：boundary reflection 从“所有 boundary 二跑”收敛为低置信、证据冲突、prefilter/brain 冲突或中等 first-brain 信号才触发；clear first-brain boundary 直接用 first brain + metadata 收口。
- ✅ `auto/prefilter.py` / `qa.py`：prefilter 降低泛词单词权重，对缺摘要 strong 候选降级；QA 增加 `prefilter_too_permissive`、`prefilter_too_strict`、`prefilter_hard_reject_dominates`。
- ✅ `discovery.py` / `sources/registry.py` / `qa.py`：按用户/配置 source 顺序执行；`SourcePlugin` 协议扩展 `SourceQuery` / `SourceResult` / `plan_queries` / `fetch` / `normalize`；source preflight 写入 `.papercompass/manifests/source_preflight_latest.json` 并进入 QA warning。
- ✅ `sources/arxiv.py` / `sources/openalex.py` / `tests/test_source_registry.py`：arXiv 与 OpenAlex 插件已实现可单测的 query planning、fetch、normalize；所有内置 source 对象都暴露完整 SourcePlugin 协议方法，未迁移 source 明确保留稳定 sync 兼容层。
- ✅ `update.py`：模式文案收敛为 checkpointed full rebuild with identity delta；discovery checkpoint 改为 source/query 粒度；每次 update 写 `commit.json`，记录 QA phase、checkpoint 是否提交及 data/catalog manifest 前后状态。
- ✅ `doctor.py` / `metrics.py` / `cli.py`：新增 `papercompass doctor workspace`、`papercompass doctor archive`、`papercompass monitor summary`；discover/update/auto-build 追加 `.papercompass/metrics/runs.jsonl`。
- ✅ `scripts/make_source_zip.py` / `scripts/check_source_archive.py` / `MANIFEST.in`：新增白名单源码包构建与 secret/local artifact scan，排除 `.claude`、pycache、pytest cache、egg-info、历史审计目录和疑似密钥。
- ✅ `.github/workflows/ci.yml` / `tests/test_text_storage.py` / `scripts/test_source_archive.py`：CI 扩展到 Linux/macOS/Windows × Python 3.11/3.12/3.13，并新增跨进程 workspace lock 超时测试；archive secret scan 与解压后 pytest 纳入 CI。
- ✅ 回归测试：新增/更新 confirmation fresh、workspace context hash、lock timeout、跨进程锁、OpenAI-compatible Brain 配置与 429、prefilter 缺摘要降级、prefilter QA warning、source preflight manifest、doctor/archive/monitor 等测试。
- ✅ 验证：
  - 目标回归：`uv run --no-sync pytest -q tests/test_source_registry.py tests/test_discovery.py tests/test_update.py` -> `57 passed`。
  - 全量测试：`uv run --no-sync pytest -q` -> `307 passed, 3 deselected`。
  - 前端语法与空白：`node --check src/papercompass/static/app.js`、`git diff --check` 均通过。
  - 源码包：`uv run --no-sync python scripts/make_source_zip.py 'gpt-pro意见/PaperCompass_source.zip'` -> `113` files；`check_source_archive.py` -> `status=passed`, `bad_entry_count=0`, `secret_hit_count=0`；`test_source_archive.py` -> `303 passed, 4 skipped, 3 deselected`（sanitized archive 不包含 1GB 的 `examples/`，对应 contract tests 跳过）。

### 2026-06-21 S5：第五轮 GPT-Pro 审计全部建议收尾（完成）

对应 `../gpt_pro_raw_review.md` 的“第五轮 GPT-Pro 审计答复”。

- ✅ **源码包发布卫生闭环**：
  - `scripts/make_source_zip.py` 白名单纳入 `scripts/` 与 `.github/`，源码包可审计打包脚本和 CI 逻辑。
  - `doctor_archive()` 禁止 `.egg-info`，扩大 secret scan 到 `.env/.ini/.cfg/.conf/.lock` 与无后缀可解码文本文件；新增裸高熵 token 检测、常见 hash/示例/env 名称 allowlist 和 2MB 单文件读取上限。
  - `doctor archive --strict` 会把大文本跳过扫描视为失败；`scripts/check_source_archive.py` 复用同一 doctor 逻辑。
- ✅ **Workspace 锁与 flaky 测试收口**：
  - `text.py` 的 `_path_file_lock()` 改为进程内 RLock 与 OS 文件锁共享同一个 deadline，避免 timeout 被串联放大。
  - `tests/test_text_storage.py` 的跨进程锁测试等待窗口从 2 秒提升到 10 秒，并把子进程异常回传 queue，区分“子进程未启动”和“锁未阻塞”。
- ✅ **两阶段确认与 update 副作用边界**：
  - `run_workspace_update()` 在 `ensure_workspace_dirs()` 前完成 confirmation token 校验；无 token 的新 workspace update 不再创建目录。
  - `tests/test_update.py` 新增 `test_update_without_token_has_no_side_effects_on_new_path`。
- ✅ **Update rollback 事务边界**：
  - `update.py` 在本次 update 目录写 `backup_before/`，备份 `.raw/data/catalog`；QA failed 或异常时把污染产物移动到 `failed_artifacts/` 并恢复 before 快照。
  - `commit.json` / `summary.json` 记录 `artifact_transaction` 与 `rollback`，同时保持 checkpoint 只在 QA 非 failed 后提交。
  - `tests/test_update.py` 新增 QA failed rollback 回归测试。
- ✅ **Deterministic Prefilter 改进**：
  - `auto/prefilter.py` 将正向词拆为 `exact_phrases`、非泛词 `soft_tokens` 与 `generic_tokens`；phrase signal 不再被 `model/data/paper` 等泛词污染，soft token 只提供受控召回。
  - strong audit sample 改为分层 deterministic sampling，不再只抽最高分头部样本，便于估计 strong 分区误收风险。
  - 新增独立 `papercompass prefilter --workspace ... --prepare -> --confirmed-token` 命令，明确 `build=normalize/dedupe`，`score_papers=prefilter+LLM review queue`。
- ✅ **Review Cache v2 / Reflection 收口**：
  - `stage_resolve_boundary()` 的 reflection 触发条件收敛为低置信、证据冲突、prefilter/brain 冲突、brain 自身接近边界，或总分近边界且 brain/confidence 也不够强；`brain_score=65`、`confidence=0.65` 的 clear first-brain boundary 不再触发 reflection。
  - `boundary_partial.jsonl` 改为保存并校验 v2 key：`cache_key`、`candidate_fingerprint`、`topic_context_hash`、reflection prompt/schema/policy version、brain 和 model，避免 topic/model/prompt 变化后误复用旧 partial。
- ✅ **OpenAI-compatible Brain 生产配置**：
  - `OpenAICompatibleBrain` 支持 `PAPERCOMPASS_BRAIN_INPUT_PRICE_PER_MTOK` 与 `PAPERCOMPASS_BRAIN_OUTPUT_PRICE_PER_MTOK`，任意兼容 API 都能把 usage 估算成 cost。
  - 文档补充 `json_object -> none` 的 response_format 降级建议。
- ✅ **SourcePlugin 第二阶段切口**：
  - `discovery.py` 新增 `run_structured_source_plugin()`，arXiv 主路径已走 `plan_queries -> fetch -> normalize` runner，并写 coverage、source run、raw segments 与 `checkpoint_updates`。
  - 其他 source 仍保留稳定 sync 兼容层；OpenAlex 已有 plan/fetch/normalize 单测，后续可按同一 runner 迁移。
- ✅ **Doctor / monitor / metrics 增强**：
  - `doctor_workspace()` 新增 review cache staleness report：`entries`、`current_match_count`、`stale_count`、`stale_ratio`。
  - `doctor workspace --fix` 只移动低风险 orphan tmp、catalog swap dir 与过期 confirmation token 到 `.papercompass/trash/doctor_<stamp>/`，不自动修 raw JSONL、manifest mismatch 或 stale decisions。
  - `monitor metrics` 输出最近 run metrics；`monitor cost` 汇总 LLM token 与 cost。
  - `record_run_metric()` 统一写入 `run_id`、`command`、`started_at`、`finished_at`、`duration_seconds`、remote calls、LLM tokens/cost、review cache hit/miss、prefilter 和 QA 字段；auto-build 不再写空 started/finished。
- ✅ **CI / 发布验证**：
  - `.github/workflows/ci.yml` 扩展到 Python 3.10/3.11/3.12/3.13 × Linux/macOS/Windows，保留 pytest、source archive doctor 和解压后 pytest。
  - `scripts/test_source_archive.py` 验证 archive 解压后可独立 `pytest -q`。
- ✅ **文档与 Agent SOP**：
  - `README.md`、`AGENT_ENTRY.md`、`docs/core-pipeline.md`、`docs/commands.md`、`docs/auto-build.md`、`docs/maintenance.md` 已同步 update rollback、prefilter 命令、doctor fix/strict、monitor metrics/cost、OpenAI-compatible price env 与源码包白名单范围。
- ✅ **验证结果**：
  - 针对性回归：`uv run --no-sync pytest -q tests/test_doctor.py tests/test_prefilter.py tests/test_brain_plugin.py tests/test_text_storage.py tests/test_update.py tests/test_source_registry.py tests/test_score_pipeline.py` -> `47 passed`。
  - 当前工作树全量：`uv run --no-sync pytest -q` -> `315 passed, 3 deselected`。
  - 源码包：`uv run --no-sync python scripts/make_source_zip.py PaperCompass_source.zip` -> `111` files。
  - 源码包检查：`uv run --no-sync python scripts/check_source_archive.py PaperCompass_source.zip` -> `status=passed`, `bad_entry_count=0`, `secret_hit_count=0`。
  - strict archive doctor：`uv run --no-sync papercompass doctor archive PaperCompass_source.zip --strict` -> `status=passed`, `large_text_skipped_count=0`。
  - 解压源码包测试：`uv run --no-sync python scripts/test_source_archive.py PaperCompass_source.zip` -> `311 passed, 4 skipped, 3 deselected`。

### 2026-06-21 S6：第六轮 GPT-Pro hardening 全部建议收尾（完成）

对应 `../gpt_pro_raw_review.md` 的“第六轮 GPT-Pro 审计答复”。

- ✅ **Doctor --fix 并发安全与保守修复边界**：
  - `doctor_workspace(fix=True)` 外层持有 `workspace_lock()`。
  - `doctor --fix` 只移动 mtime 超过安全阈值的 orphan `.tmp` 与 `.catalog.*`，避免误伤 active atomic-write tmp 或 catalog swap。
  - 新增 `--prune-updates` 与 `--keep-success-backups`，只移动成功 update run 的旧 `backup_before/` 到 `.papercompass/trash/`，不移动 failed artifacts。
- ✅ **Update 从 backup-restore 升级为 staged publish**：
  - `run_workspace_update()` 先复制必要输入到 `.papercompass/updates/update_<id>/staged_workspace/`，在 staged workspace 内执行 discover/build/catalog/QA。
  - QA 未失败后才发布 staged `.raw/data/catalog` 回主 workspace，并提交 discovery / identity checkpoints；成功后清理 `backup_before/` 与 staged workspace。
  - QA failed 时不发布 staged 产物，主 workspace 的 `.raw/data/catalog` 保持运行前状态；`commit.json` 明确记录 `rollback_scope`、`audit_artifacts_retained`、`publish` 与 `rollback` 语义。
- ✅ **Metrics / monitor 真实贯通**：
  - `stage_score_papers()` 返回并记录 `review_cache_hit_count` / `review_cache_miss_count` / `partial_cache_hit_count`。
  - `auto-build` metrics 不再硬编码 `remote_calls_used=0`，而是从 discover stages 汇总。
  - `stage_resolve_boundary()` 记录 `reflection_count`、`direct_resolved_count` 与 `reflection_trigger_reasons`，并写入 auto-build metrics。
  - 新增 `papercompass monitor trends`，汇总 LLM cost、remote calls、review cache hit rate、prefilter LLM review ratio、source errors / source failure rate，并输出 `llm_cost_budget_exceeded`、`review_cache_hit_rate_low`、`prefilter_llm_review_ratio_high`、`remote_calls_near_limit`、`source_errors_recent` 等告警码。
- ✅ **低层写命令统一 workspace lock**：
  - CLI 层对 `import-papers`、`import-saved-search`、`import-agent-search`、`review-feedback import`、`agent-search record`、`agent-run log`、`add-paper`、`sync`、`prefilter`、`override add`、`fulltext fetch` 包裹 workspace lock。
- ✅ **SourcePlugin 第二阶段迁移**：
  - `run_discovery()` 中 arXiv、OpenAlex、Semantic Scholar 主路径均走 `run_structured_source_plugin()`。
  - `SemanticScholarSourcePlugin` 新增结构化 `plan_queries()` / `fetch()` / `normalize()`，保留旧 `run()` 兼容层。
  - 文档更新为当前事实：registry/protocol 已实现，arXiv/OpenAlex/Semantic Scholar structured runner 已落地；其他内置源继续按兼容层逐步迁移。
- ✅ **源码包安全与 release gate**：
  - `doctor_archive()` 将 `../`、绝对路径和 Windows drive path 视为 bad entry。
  - `scripts/test_source_archive.py` 从 `extractall()` 改为 `safe_extract()`，防 Zip Slip。
  - `scripts/check_source_archive.py` 新增 `--strict`。
  - `scripts/make_source_zip.py` 生成 `PaperCompass_source.zip.manifest.json` sidecar，记录版本、git commit、文件数、sha256 与 release gate 命令。
  - CI 增加 strict archive check；现有 Linux/macOS/Windows 矩阵继续运行跨进程 workspace lock 压测。
- ✅ **文档 / SOP 同步**：
  - `README.md`、`AGENT_ENTRY.md`、`docs/commands.md`、`docs/core-pipeline.md`、`docs/maintenance.md` 和 `skills/shared-references/source-plugin-protocol.md` 同步 staged update、doctor fix/prune、monitor trends、source plugin 当前状态、archive manifest 与 release gate。
- ✅ **验证结果**：
  - 目标回归：`uv run --no-sync pytest -q tests/test_doctor.py tests/test_update.py tests/test_source_registry.py tests/test_discovery.py tests/test_score_pipeline.py tests/test_auto_orchestrator.py tests/test_source_archive_scripts.py` -> `109 passed`。
  - 当前工作树全量：`uv run --no-sync pytest -q` -> `322 passed, 3 deselected`。
  - 前端语法与空白：`node --check src/papercompass/static/app.js`、`git diff --check` 均通过。
  - 源码包：`uv run --no-sync python scripts/make_source_zip.py 'gpt-pro意见/PaperCompass_source.zip'` -> `112` files，并生成 `gpt-pro意见/PaperCompass_source.zip.manifest.json`。
  - 源码包检查：`uv run --no-sync python scripts/check_source_archive.py 'gpt-pro意见/PaperCompass_source.zip'` -> `status=passed`, `bad_entry_count=0`, `secret_hit_count=0`。
  - strict archive doctor：`uv run --no-sync python scripts/check_source_archive.py --strict 'gpt-pro意见/PaperCompass_source.zip'` -> `status=passed`, `large_text_skipped_count=0`。
  - 解压源码包测试：`uv run --no-sync python scripts/test_source_archive.py 'gpt-pro意见/PaperCompass_source.zip'` -> `318 passed, 4 skipped, 3 deselected`。

### 2026-06-21 S7：第七轮 GPT-Pro release hardening 收口（完成）

对应 `../gpt_pro_raw_review.md` 的“第七轮 GPT-Pro 审计答复”。

- ✅ **Publish + checkpoint 失败事务边界**：
  - `run_workspace_update()` 在发布 staged `.raw/data/catalog` 前备份 discovery / identity checkpoints。
  - staged 产物发布成功后，如果 discovery 或 identity checkpoint 任一写入失败，会立即恢复 `.raw/data/catalog` 的 `backup_before/`，并恢复或移除本次写到一半的 checkpoint 文件。
  - 失败路径保留 `commit_checkpoint_failed.json`、`failed_artifacts/`、`failed_checkpoints/` 与 staged workspace，便于审计，不会把半提交状态伪装成成功 update。
- ✅ **源码包 symlink / root escape 防护**：
  - `scripts/make_source_zip.py` 跳过 symlink。
  - 对每个候选文件执行 `resolve().relative_to(root)` 校验，resolved path 不在项目 root 内的文件不会进入 archive。
- ✅ **Doctor trash orphan 排除**：
  - `doctor_workspace()` 的 `.tmp` orphan 扫描排除 `.papercompass/trash/`，避免 `doctor --fix` 将 orphan 移入 trash 后下一轮又报告 trash 内 `.tmp`。
- ✅ **Archive safe extraction 回归增强**：
  - `tests/test_source_archive_scripts.py` 除 `../outside.py` 外，新增 `safe/../../outside.py` 嵌套路径穿越回归。
  - 新增源码包 symlink-to-outside 回归测试。
- ✅ **核验无需改动项**：
  - `atomic_write_text()` 当前只有一次 `_fsync_dir(path.parent)`，未发现第七轮提到的重复 fsync。
  - `rg -n "portalocker" README.md AGENT_ENTRY.md docs skills src tests scripts pyproject.toml` 无结果，文档中没有残留 portalocker 误导表述。
  - CLI confirmation token 先消费再尝试命令锁的问题保留为可接受 UX tradeoff；第七轮明确该项不阻塞 Release 0.1，本轮未引入更大改动面。
- ✅ **验证结果**：
  - 目标回归：`uv run --no-sync pytest -q tests/test_update.py tests/test_doctor.py tests/test_source_archive_scripts.py` -> `23 passed`。
  - 当前工作树全量：`uv run --no-sync pytest -q` -> `326 passed, 3 deselected`。
  - 语法与空白：`python -m py_compile src/papercompass/update.py src/papercompass/doctor.py scripts/make_source_zip.py scripts/test_source_archive.py tests/test_update.py tests/test_doctor.py tests/test_source_archive_scripts.py`、`node --check src/papercompass/static/app.js`、`git diff --check` 均通过。
  - 源码包：`uv run --no-sync python scripts/make_source_zip.py 'gpt-pro意见/PaperCompass_source.zip'` -> `112` files，并生成 `gpt-pro意见/PaperCompass_source.zip.manifest.json`。
  - 源码包检查：`uv run --no-sync python scripts/check_source_archive.py 'gpt-pro意见/PaperCompass_source.zip'` -> `status=passed`, `bad_entry_count=0`, `secret_hit_count=0`。
  - strict archive doctor：`uv run --no-sync python scripts/check_source_archive.py --strict 'gpt-pro意见/PaperCompass_source.zip'` -> `status=passed`, `large_text_skipped_count=0`。
  - 解压源码包测试：`uv run --no-sync python scripts/test_source_archive.py 'gpt-pro意见/PaperCompass_source.zip'` -> `322 passed, 4 skipped, 3 deselected`。
