# PaperCompass 改进 — 实施进度

> 配套方案见同目录 `元数据与召回改进-方案与进度.md`。本文件**每完成一个阶段即更新**,直到全部完毕。
> 分支:`improve/metadata-recall-budget` · 基线:103 测试通过(改动模块子集)

状态图标:⬜ 未开始 · 🟦 进行中 · ✅ 完成并通过测试 · ⏸ 暂缓

---

## 总进度

| 阶段 | 任务 | 状态 | 备注 |
| --- | --- | --- | --- |
| P0 | B2 去硬编码桥接词 | ✅ | 改为保留特性、只在 in-scope 触发(见 B1) |
| P0 | B1 axis 只扫 in-scope | ✅ | `plan.py: _in_scope_text` + 2 处调用 + 回归测试 |
| P0 | A1 merge 源优先级 | ✅ | `normalize.py`: 源优先级 venue + 派生 `publication_status` |
| P0 | C1 门控去 oversell + 自适应批次 | ✅ | `quality_gate.py`/`orchestrator.py` |
| P1 | B3 通用词 denylist | ✅ | `plan.py`: `_DENY_STANDALONE_RECALL_TERMS` + 过滤 |
| P1 | B4 召回词体检 | ⏸ 暂缓 | 需 plan 阶段联网,B3+泛锚点门已覆盖,见下 |
| P1 | A2 接收状态一等公民 | ✅ | `scope.py`: `acceptance` 策略门 |
| P1 | A3 arXiv 合法 venue | 🟦 部分 | scope 侧已放行 preprint;discovery 侧链接 arXiv 未做 |
| P2 | C2 加预算 resume | ⏸ 暂缓 | C1 自动提批已消除常见场景,见下 |
| P2 | C3 进度/状态文案 | 🟦 部分 | `batch i/N` 已;brain_missing_scores 拆分/状态人话未做 |
| 可选 | T3 实体融合层 | ⏸ 暂缓 | A1 已用收尾融合解决主要错标,大改另排 |

---

## 变更日志

### 2026-06-19
- 创建分支 `improve/metadata-recall-budget`,基线测试 103 通过。
- 开始实施 P0。
- ✅ **B1+B2**:`plan.py` 新增 `_in_scope_text()`,在 `_coverage_axis_aliases` 的两处调用前把 direction 截到 in-scope 段。**决策(取舍)**:不删除作者有意设计的跨轴桥接词(B2),只阻止"排除条款里的词"误触发——既修了 ICL 误注入,又保留原特性。新增回归测试 `test_out_of_scope_agent_mention_...`。`test_auto_plan.py` 21 通过。
- ✅ **A1**:`normalize.py` 在 dedup 收尾处新增 `_finalize_venue()`(按源可信度选 venue,published 源 > 投稿追踪源,preprint venue 兜底)+ `_finalize_publication_status()`(派生可信发表状态:explicit accept / published 证据 > OpenReview Reject)。新增字段 `publication_status`(additive)。3 个回归测试(含顺序无关)。**全量 236 测试通过**。
  - 已知边界:若某论文只有 paperlists 的被拒投稿记录、缺已发表记录(如 TMLR 那篇),merge 无法凭空补出发表状态——需 A3/更好链接把已发表记录召回进来,不在 A1 范围。
- ✅ **C1**:(1) `quality_gate.py` 把 `weak_queue_too_large` 从 `rule_repair` 降级为 `budget`(真正"规则坏了"的信号是 `generic_anchor_dominates`);加 `recommended_max_batches` + 报告里给"重跑加 --weak-max-batches N"。(2) `orchestrator.py` 在门控前**自动提批**:`needed>weak_max_batches` 且 `<=80` 时把 `weak_max_batches` 提到 needed 并记 `weak_budget_autoraised` 事件——常见的"默认批次太小"从 exit5 失败变成透明成功。(3) `weak_queue_too_large` 从 `RULE_REPAIR_WARNINGS` 移到 `BUDGET_WARNINGS`。新增门控测试(大而干净的队列=budget 而非 rule_repair)。**全量 237 通过**。

> **✅ P0 全部完成(B1/B2/A1/C1),237 测试通过。**
- ✅ **A2+A3**:`scope.py` 新增 `acceptance` 策略门。默认 `accepted_or_preprint`:丢弃 `publication_status ∈ {rejected,withdrawn,desk_reject}`(venue 是投稿目标≠接收,这一步堵住"被拒投稿凭 venue 混进来");`accepted_only` 额外要求 venue 命中背后有接收证据;`any` 恢复旧行为。preprint 仍经 `include_preprints` 放行(对应"arxiv 较新也接受")。向后兼容(无 `publication_status` 的旧论文不受影响)。4 个回归测试。
- ✅ **B3**:`plan.py` 新增 `_DENY_STANDALONE_RECALL_TERMS`(scaling laws / bayesian inference / gradient descent / attention / transformer …),在 `recall_terms_for_plan` 末尾过滤——这些词不得**单独**成召回锚点,但 topic 复合词(如 "scaling laws of in-context learning")不受影响。回归测试。
- ✅ **C3**:`stages.py` 把 `batch i/batches_done`(恒为 i/i)改成 `batch i/total_batches`(真实总批数),score_papers 与 resolve_boundary 各两处;`batches_done` 的收尾语义保持不变。
- ⏸ **暂缓项(附理由,非静默丢弃)**:
  - **B4 召回词体检**:需在 plan 阶段对每个 source_filter_term 跑小样本联网召回,慢且测试不稳定;B3 denylist + 现有 `generic_anchor_dominates` 门已覆盖观测到的过泛问题。边际价值低。
  - **C2 加预算 resume**:C1 的自动提批已让"默认批次太小"直接成功,常见场景不再需要 resume;干净的"复用已检索候选 + 提预算重跑"是状态机较大改动,留待需要时做。
  - **T3 实体融合层**:A1 的收尾融合已让 venue/接收状态顺序无关地正确;完整 work-entity 层是大改,收益边际,按方案标记可选。

---

## ✅ 全部完毕(本轮范围)

- 完成:**B1 B2 A1 C1 A2 B3**(P0 全部 + P1 主体);**A3 / C3 部分完成**(各剩一块,见上)。
- 暂缓(带理由):**B4 C2 T3** + A3/C3 的剩余块——均为大改/联网/边际项或彼此重叠,根因已被已完成项覆盖。
- 测试:基线 103 → 全量 **241 通过**(新增 9 个回归测试:plan 2、normalize 3、scope 4、quality_gate 1,去重后净增覆盖)。改动 6 源文件 + 4 测试文件,+381/−10。
- 分支:`improve/metadata-recall-budget`(未提交,等你 review;需要我 commit/PR 再说)。
- 如要继续 B4/C2/T3,说一声即可。
